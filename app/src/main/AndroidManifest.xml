<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.seniorjobapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SeniorJobApp">

        <activity android:name=".SignupActivity" />


        <activity android:name=".JobRegisterActivity" />
        <activity android:name=".MapSelectActivity"/>
        <activity android:name=".ApplicantStatusActivity"/>



        <activity
            android:name=".BusinessMenuActivity"
            android:exported="false" />
        <activity
            android:name=".WorkerActivity"
            android:exported="false" />
        <activity android:name=".WorkerJobListActivity" />

        <!-- 🔽 여기에 추가 -->
        <activity android:name=".JobStatusActivity" />

        <activity
            android:name=".Activity02"
            android:exported="false" />

        <!-- SplashActivity를 앱 시작화면으로 지정 -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SeniorJobApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />
        <activity android:name=".SelectRoleActivity" />
        <activity android:name=".SelectJobTypeActivity" />
        <activity android:name=".MapSelectActivity" />

    </application>


</manifest>